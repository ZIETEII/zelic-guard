# Optional server-only OpenAI compiler. The judge path works without credentials.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Optional stateless operator workspace. Keep every value server-only.
ZELIC_AUTH_EMAIL=judge@zelic.guard
ZELIC_AUTH_PASSWORD_SCRYPT=
ZELIC_SESSION_SECRET=
